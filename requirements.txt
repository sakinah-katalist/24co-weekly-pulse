# Bounded ranges: let pip pick versions compatible with the deploy
# environment's Python, while capping majors to avoid untested
# freshly-released wheels (an uncapped install segfaulted on
# Streamlit Cloud on 15 Jul 2026).
streamlit>=1.44,<2
pandas>=2.2,<3.1
numpy>=1.26,<3
matplotlib>=3.8,<3.11
pillow>=10,<13
pyarrow>=17,<25
reportlab>=4.2,<5
notion-client>=2.2.1
